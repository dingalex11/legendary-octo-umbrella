Phase 1: Frontend Firebase Initialization & AuthenticationCurrently, your app relies purely on local state. We need to introduce Firebase and a way for users to log in.index.html Updates:SDK Imports: Add the Firebase Modular SDK (version 12+) via CDN to the <head> or bottom of your <body>. You will need the core firebase-app, firebase-auth, and firebase-firestore modules.  Auth UI: Add a new "Account" or "Login" modal to your UI. This modal should include fields for Email and Password, and buttons for "Sign In" and "Sign Up".  Header Updates: Modify the top navigation bar to display the logged-in user's email and a "Sign Out" button once they are authenticated. Add a "Settings" button to open a modal where they can paste their personal Groq API key.  New File (auth.js):Create a new script to hold your Firebase configuration object.Implement an authentication state observer (onAuthStateChanged) to track if a user is logged in.Map the UI buttons from index.html to Firebase Auth functions (signInWithEmailAndPassword, createUserWithEmailAndPassword, signOut).Phase 2: User Settings & API Key RoutingCurrently, the JudgeService relies on a global environment variable for the Groq API key. We need to securely route the user's specific key to the backend.  auth.js (Frontend):When a user inputs their Groq API key into the Settings modal, save it to their specific Firestore document path (/users/{uid}).ui.js & websocket.js (Frontend):When the user joins a room via joinRoom() or initiates the WebSocket connection, fetch their Groq API key from Firestore.  Send this key securely to the backend. You can either pass it as a query parameter in the WebSocket URL (/ws/{room_id}?key=...) or send it as the very first AUTH_INIT event payload as soon as the connection opens.  ui_service.py & main.py (Backend):Update the RoomManager and ScienceBowlModerator initializers to accept the user's Groq API key.  Pass this specific key into the JudgeService initialization rather than relying strictly on the .env fallback.judge_service.py (Backend):Update the __init__ method to accept the API key as an argument and apply it to the AsyncOpenAI client.  Phase 3: Cloud Packet StorageCurrently, the PDF parser generates a JSON file that the user must download and re-upload. We will reroute this to Firestore.  pdf_parser.js:Add a "Save to Cloud" button next to the existing "Download JSON" and "Copy" buttons.  When clicked, push the parsed currentParsedData array to Firestore under /users/{uid}/packets/{packet_id}.  ui.js (Setup View):In the Setup tab (#view-setup), alongside the local JSON file browser, add a "Load Cloud Packet" dropdown.  When the Setup tab is opened, query Firestore for all packets owned by the logged-in user and populate this dropdown.When a packet is selected, trigger the existing LOAD_BANK event to send the cloud data directly to the Python backend.  Phase 4: Match Logs & Analytics PersistenceCurrently, match logs are kept in Python memory, exported as a CSV, and manually dragged into the Analytics dashboard. We will automate this pipeline.  main.py (Backend):When a match concludes, or periodically throughout the match, emit a new event type (e.g., MATCH_COMPLETE or SYNC_LOG) containing the full self.match_log array.  websocket.js (Frontend):Listen for this new log sync event.  When received, use the Firebase SDK to write the entire log payload to Firestore under /users/{uid}/match_logs/{room_id}.analytics.js (Frontend):In the Analytics Dashboard (#view-analytics), add a "Load Cloud Match History" button alongside the drag-and-drop CSV zone.  When clicked, query Firestore for the user's saved match logs, reformat them if necessary, and inject them directly into your existing processCSVData logic so the tables populate instantly.  